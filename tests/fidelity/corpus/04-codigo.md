# Código

Inline `const x = 1` y bloque:

```python
def hola(nombre: str) -> str:
    return f"Hola {nombre}"
```

- Lista con código:

  ```bash
  git -C ~/Projects/lumen-md status
  ```

- Otro ítem
